To import the dataset, use the cypress_bdd.sql file located in "\dataset". The same folder has an Examples.sql containing some example queries that might be useful.

You can also check individual files in the "\BDD" and "\cypress\e2e" folders
